# Runbook

## Local setup

1. Install dependencies
2. Build content index
3. Build embeddings
4. Run the dev server

## Environment variables

- `OPENAI_API_KEY`
- `OPENAI_MODEL`
- `EMBEDDING_MODEL`

## Deployment checks

- content files present
- AI context explainers present for every role
- analytics snippet configured
- no raw JD logging
